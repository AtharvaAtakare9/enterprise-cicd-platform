# Example only — do NOT commit real secrets.
# Create for real via: kubectl create secret generic expense-tracker-secrets \
#   --from-literal=DATABASE_URL=postgresql://user:pass@host/db \
#   --from-literal=JWT_SECRET=changeme
apiVersion: v1
kind: Secret
metadata:
  name: expense-tracker-secrets
type: Opaque
stringData:
  DATABASE_URL: postgresql://user:pass@host:5432/expense-tracker
  JWT_SECRET: changeme
  JWT_EXPIRY: 1d
  NODE_ENV: production
