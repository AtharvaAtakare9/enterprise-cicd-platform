apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: expense-tracker-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Andacanaver/expense-tracker-backend.git
    targetRevision: main
    path: k8s/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: expense-tracker-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        maxDuration: 1m
